apiVersion: v1
kind: ConfigMap
metadata:
  name: db-config
  namespace: nagp
  labels:
    app: postgres
data:
  POSTGRES_USER: "postgres"
  POSTGRES_DB: "postgres"
  PGDATA: "/var/lib/postgresql/data/pgdata"
